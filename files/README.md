# Generated Condition Results

Condition-search result text files are written here by default.

The generated `condition*_stock_*.txt` files are ignored by Git because they are runtime outputs. Set `STOCK_COLLECT_OUTPUT_DIR` to write them somewhere else.
